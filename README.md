# יהודה שיפוצים — PWA הצעות מחיר · v12.4

יהודה יעקובוב · 054-5718316 · ח.פ 311834998 · yuda090581@gmail.com
yehuda-shipuzim.netlify.app · Firebase: yehuda-shipuzim · מסמך סנכרון: yehuda

## v12.4 — מסך מעקב

### סינון בלחיצה
ארבעה כרטיסי סיכום — נשלחו · נצפו · התלבטו · אושרו.
לחיצה מסננת את הרשימה לאותה קבוצה; לחיצה חוזרת מבטלת.
הכרטיס הפעיל מסומן במסגרת אדומה עם ✕.

### חיפוש חופשי
שם לקוח · מספר הצעה · טלפון · תיאור פרויקט.
מתעלם מרווחים, מקפים וגרשיים — "050-123-4567" מוצא את אותו לקוח כמו "0501234567".
הקלדה עם השהיה של 260ms, הסמן נשמר במקומו.

### מיון
הכי חדש · סכום גבוה · הכי דחוף (דגלי התראה קודם).

### תיקונים
- **כפילויות** — אותה הצעה שנשלחה שוב קיבלה שני כרטיסים נפרדים.
  עכשיו מופיע תג "נשלח ×2" וכפתור 🧹 מזג שמשאיר את הכרטיס עם
  ההתקדמות הרבה ביותר (אושר > נצפה > חדש).
- **התראת "זמן להתקשר"** — הציגה שורה לכל לינק, כלומר אותו לקוח 9 פעמים.
  עכשיו לקוח אחד = שורה אחת, עד 5 שורות + "ועוד N".
- **סכומים** — `37,122.8 ₪` הפך ל-`37,123 ₪` (nisR).
- **מצב ריק** — "אין תוצאות" עם כפתור ניקוי, במקום מסך ריק.

## גרסאות קודמות
- **v12.3** — סעיפי "בדוק סעיפים חסרים" נוצרו בלי `id` ולכן היו בלתי ניתנים
  לעריכה ולחיצה עליהם זרקה TypeError. תוקן + מיגרציה + דדופ + מחירים
  מה-AI + שמירה קבועה בקטגוריה.
- **v12.2** — המחירון (240 סעיפים) נשלח בפרומפט עם prompt caching;
  ה-AI מחזיר `ref` במקום להמציא שמות ומחירים. Gemini ראשי, Claude גיבוי.
- **v12.1** — `claude-3-5-haiku-latest` הוסר מה-API של Anthropic והוחלף
  ב-`claude-haiku-4-5-20251001` (+ `claude-sonnet-5` לקריאת PDF).
  שכבת `AI` מרכזית, שרשרת נפילה, אודיו מנותב ל-Gemini תמיד,
  11 שערי מפתח תוקנו ל-`AI.anyKey()`.
- **v12.0** — מחירון הוכפל ל-240 · סטטוסים אוחדו · `_headers`

## מבנה
`index.html` יחיד + `sw.js` + `version.json` + `manifest.webmanifest` + 3 אייקונים + `_headers`.
בלי build, בלי npm.

## אבטחת מפתח
המפתחות בקוד לקוח וניתנים לחילוץ מהדפדפן.
מומלץ spend limit חודשי בקונסול של Anthropic.

## חוקי עבודה
כל שינוי → בדיקת JS + איזון div → VERSION ב-index + CACHE ב-sw.js + version.json → ZIP.
חלק מהטקסטים בקוד כתובים כ-escape sequences — החלפה רגילה לא רואה אותם.
אסור לכתוב "קבלן רשום" בשום מקום.

## הבא בתור
ארתור v2.0 — בנייה מ-v12.2 (מוקפא עד סגירת מעגל מול יהודה)
