# יהודה שיפוצים — אפליקציית אנדרואיד מתעדכנת

חבילה זו היא אפליקציית ווב מתקדמת (PWA) שהופכת לאפליקציית אנדרואיד אמיתית שמתעדכנת לבד.

## מה יש בחבילה
- `index.html` — האפליקציה
- `sw.js` — Service Worker (עבודה אופליין + עדכון אוטומטי)
- `manifest.webmanifest` — הגדרות האפליקציה והאייקונים
- `icon-192.png` / `icon-512.png` / `icon-180.png` — אייקונים
- `version.json` — מספר גרסה לבדיקת עדכונים

---

## שלב 1 — פרסום לאינטרנט (Netlify · חינם)
1. היכנס ל-app.netlify.com → Add new site → Deploy manually.
2. גרור את **כל התיקייה הזו** (או קובץ ZIP שלה) לאזור ההעלאה.
3. קיבלת כתובת כמו `https://yehuda-shipuzim.netlify.app`.
   (אפשר לשנות שם ב-Site settings → Change site name.)

מרגע זה האפליקציה חיה. אפשר כבר "הוספה למסך הבית" מכרום והיא תעבוד כאפליקציה.

## שלב 2 — יצירת קובץ APK (PWABuilder · חינם)
1. היכנס ל-**pwabuilder.com**.
2. הדבק את כתובת ה-Netlify שלך → Start.
3. לחץ **Package For Stores** → **Android** → **Generate**.
4. הורדת קובץ `.apk` (להתקנה ישירה) ו-`.aab` (לחנות Play).
5. התקן את ה-APK בטלפון, או העלה את ה-AAB ל-Google Play Console.

## שלב 3 — עדכונים (זה החלק שמתעדכן לבד)
- ה-APK הוא עטיפה שטוענת את האתר החי שלך. **כל שינוי שתעלה ל-Netlify מופיע אוטומטית באפליקציה** בפתיחה הבאה — בלי להתקין מחדש.
- כשאני נותן לך גרסה חדשה: החלף את `index.html` ב-Netlify, ועדכן את מספר הגרסה בשני מקומות:
  1. בתחילת `sw.js` בשורה `const CACHE = 'ys-cache-vX';`
  2. בקובץ `version.json`
  ה-Service Worker יזהה, יתקין את הגרסה החדשה ברקע, וירענן את האפליקציה לבד.

---

## הגדרות אחרי התקנה (בתוך האפליקציה)
- **⚙️ הגדרות → GREEN-API**: הזן apiUrl, idInstance וטוקן → שליחת וואטסאפ שקטה (עותק אליך + PDF ומשוב ללקוח).
- **⚙️ הגדרות → Google Drive**: הזן Google OAuth Client ID → תיקיות תמונות "לפני/אחרי" לכל לקוח + גיבוי בענן.

בהצלחה! 🛠️

## חדש ב-v9.1 — תרגום סעיפים אמיתי 🌐
- בחירת שפה במסך ההצעה (עברית / ערבית / English / Русский) מתרגמת עכשיו גם את **הסעיפים עצמם**: שמות עבודות, קטגוריות, יחידות, חומרים ותנאי תשלום — באמצעות AI.
- ההצעה יוצאת מתורגמת בלבד (בלי עברית), גם ב-PDF וגם בלינק הדיגיטלי. פרטי הקבלן והמחירים נשארים כפי שהם.
- תרגומים נשמרים בזיכרון המכשיר (trCache) — תרגום חוזר של אותה הצעה מיידי וללא עלות AI.
- כפתור "🌐 תרגם עכשיו" לבדיקה מראש; רוסית ואנגלית מוצגות LTR, ערבית RTL.

## חדש ב-v9.2 — סטורי לפני/אחרי 🖼️
- תפריט ⚙️ ← "סטורי לפני/אחרי": קולאז' ממותג 1080×1920 לסטטוס וואטסאפ, מעוצב ב-Canvas בתוך האפליקציה (Gemini לא יוצר תמונות — לכן העיצוב מקומי).
- שני מצבים: "לפני ← אחרי" (זוג תמונות + חץ) ו-"3 שלבים" (תשתיות/ריצוף/גימור — תוויות ניתנות לעריכה).
- ✨ כפתור AI: Gemini Vision מנתח את התמונות ומציע כותרת (עם הדגשה אדומה), ושורת משנה — הכל ניתן לעריכה.
- מיתוג מלא: פס אדום, לוגו, טלפון 054-571-8316, פוטר יהודה יעקובוב + אתר. ללא "קבלן רשום" בשום מקום.
- שיתוף ישיר לסטטוס (navigator.share) או שמירה כ-JPG.

## חדש ב-v9.3 — סטורי לפי לקוח + דף נחיתה למשוב 🎬
- הוסר כפתור "📷 תמונות" מכרטיסי הלקוחות (photosModal נשאר כקוד מת מוסתר).
- סטורי לפני/אחרי עכשיו לפי לקוח: בוחרים לקוח מהרשימה, מעלים עד 4 תמונות "לפני" ועד 4 "אחרי" (גריד אוטומטי: 1/2/3/4 תמונות לכל צד).
- ה-AI (Vision רב-תמונות, עד 4) מנתח את כל התמונות + סוג הפרויקט של הלקוח ומציע כותרות.
- כפתור חדש: "📩 שלח ללקוח בקשת משוב + דף נחיתה עם הסטורי" — יוצר לינק משוב רגיל (#r=) שבו דף הנחיתה כולל את הקולאז' למעלה + כפתור שמירה למזכרת, ומתחתיו המשוב הקיים (כוכבים/טקסט/גוגל) ללא שינוי.
- הקולאז' נשמר במסמך המשוב ב-Firestore בגרסה מוקטנת (720×1280, ~150KB) כדי לעמוד במגבלת 1MB.
- מערכת המשוב הקיימת (לקוחות ← 📩 משוב) לא שונתה — עובדת בדיוק כמו קודם, בלי תמונה.
