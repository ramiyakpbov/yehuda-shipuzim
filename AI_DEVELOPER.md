# AI_DEVELOPER.md
## יהודה שיפוצים — PWA הצעות מחיר · v18.3

> מסמך העברה מלא. מודל AI חדש שקורא רק אותו יכול להמשיך את הפיתוח בלי אובדן מידע.
> **עודכן:** v18.2 · אוגוסט 2026

---

# 1 · מטרת המערכת

אפליקציית ווב מתקדמת (PWA) שבה קבלן שיפוצים בונה הצעת מחיר מקצועית בטלפון, שולח ללקוח, ועוקב אחרי מה קורה איתה עד לסגירה.

**המשתמש היחיד:** יהודה יעקובוב, קבלן שיפוצים באזור המרכז.
**המפתח והמתחזק:** רמי יעקובוב (054-3013389). הוא הלקוח שמדבר איתך, לא יהודה.

---

# 2 · הבעיה העסקית

לפני האפליקציה, יהודה כתב הצעות בוואטסאפ — טקסט חופשי, בלי מספור, בלי מעקב. התוצאות:

1. **מחירים לא עקביים** — אותה עבודה בשני מחירים לשני לקוחות
2. **הצעות נשכחות** — נשלחה, הלקוח לא ענה, ואף אחד לא חזר אליו
3. **אין נראות** — לא ידוע מי פתח, מי קרא, מי התלבט
4. **מסמך לא מקצועי** — טקסט בוואטסאפ מול PDF ממותג של המתחרים

**מה המערכת פותרת:** מחירון קבוע של 644 סעיפים · PDF ממותג בלחיצה · לינק חי שמתעד כל פעולה של הלקוח · מעקב חזרות עם תאריכי יעד · תובנות על מה עובד.

---

# 3 · ארכיטקטורת המערכת

## עיקרון־על: קובץ אחד, בלי שלב בנייה

```
index.html   719 KB   ← כל האפליקציה: HTML + CSS (109KB) + JS (608KB)
sw.js          3 KB   ← Service Worker לעבודה אופליין
version.json   17 B   ← בדיקת עדכונים
manifest.webmanifest
_headers              ← Netlify
icon-192/512/180.png
```

**זו לא בחירה טכנית — זו אילוץ תפעולי.** רמי עובד מאנדרואיד בלבד, בלי terminal ובלי npm. הפריסה היא גרירת ZIP ל-Netlify או העלאת קבצים ידנית ל-GitHub. **כל הצעה שדורשת שלב בנייה תיפסל.**

## שכבות

```
┌─────────────────────────────────────────┐
│  App  — אובייקט ענק, 216 מתודות         │
│  state (בזיכרון) → render() → innerHTML │
├─────────────────────────────────────────┤
│  Store — עטיפת localStorage עם namespace│
├─────────────────────────────────────────┤
│  Sync  — Firestore, דו-כיווני           │
├─────────────────────────────────────────┤
│  AI    — Gemini ראשי, Claude גיבוי      │
└─────────────────────────────────────────┘
```

**אין framework.** הרינדור הוא `document.body.innerHTML = this.view()` מלא בכל שינוי. זה עובד כי המסכים קטנים; אל תנסה להחליף ב-React.

## מחזור הרינדור

```js
render(){
  document.body.innerHTML = this.view();
  this.bind();          // כל המאזינים מחדש
  this.sectionizeSettings();
  this.sectionizeQuote();
  this.measureDock();
}
```

`bind()` מחבר הכל מאפס בכל רינדור. זה נראה בזבזני, אבל מונע מאזינים מיותמים.

---

# 4 · מודולים מרכזיים

## Store — אחסון עם namespace

```js
const NS='ys:';                     // ארתור: 'ar:'
const Store=(()=>{
  const K=k=>(String(k).indexOf(NS)===0?k:NS+k);
  return{ns:NS,raw:K,
   get(k,d){...localStorage.getItem(K(k))...},
   set(k,v){k=K(k); ...}, ...};
})();
```

**ה-namespace קריטי.** יהודה וארתור יושבים על אותו דומיין (`ramiyakpbov.github.io`), ולכן חולקים localStorage. בלי הקידומת הנתונים מתערבבים — זה קרה בפועל ותוקן ב-v15.8.

**סולם פינוי כשהאחסון מתמלא** (7 שלבים, ב-`Store.set`):
`ys_autobackup` → `errLog` → מטמון תרגומים → גזימת `quotes` → גזימת `sentLinks`/`reviewLinks`

## Sync — Firestore

```js
DOC = _db.collection('appdata').doc('yehuda')   // ארתור: 'artur'
```

35 מפתחות ב-`SYNC_KEYS`. **גרסה רזה של `quotes`** נשלחת (בלי snapshot מלא ובלי חתימות base64) כי Firestore מוגבל ל-1MB למסמך.

הגנה שנייה: כל רשומה נושאת `owner:'yehuda'`, והשחזור מסנן `if(v.owner&&v.owner!=='yehuda')return;`

## AI

- **Gemini** (`gemini-flash-latest`) — ראשי
- **Claude** — גיבוי, נבחר ב-`aiProvider`
- **שער כניסה:** `aiGate()` דורש קוד מנהל **3396** פעם אחת לכל הפעלה. הקוד עצמו לא בקוד המקור — רק hash ב-`PIN_FALLBACK`.

---

# 5 · מסכי המערכת

**43 מסכים.** כולם דרך `state.modal` + מפה:

```js
const map={follow:'followModal',clientcard:'clientCardModal', ... };
```

## שלוש לשוניות (`state.tab`)

| לשונית | תוכן |
|---|---|
| `home` | KPI · דורש טיפול · הצעה חדשה · הצעות אחרונות · 8 קיצורים |
| `build` | המחירון בלבד: חיפוש · כלי מנהל · 12 קטגוריות · סל |
| `track` | מעקב לינקים / כל ההצעות |

**חוק ברזל:** כל אופציה נמצאת במסך אחד בלבד. נבדק ומאומת — אפס כפילות בין הבית לתפריט. אם אתה מוסיף משהו, **בדוק קודם מה כבר מוצג שם**. זו הטעות שחזרה הכי הרבה פעמים בפרויקט.

## הקיצורים במרכז הבית (8)
`clients` · `follow` · `gallery` · `stats` · `calendar` · `wacenter` · `reviews` · `menu`

## התפריט ⚙️ (5)
`aihub` · `mystyle` · `templates` · `guide` · `settings`

---

# 6 · תהליכי עבודה

## בניית הצעה ושליחה

```
לשונית 🧮 → פתח קטגוריה → סמן סעיף → הקלד כמות
  → הסל מציג מה נבחר וכמה
  → 📄 הצעת מחיר → מלא לקוח
  → 🛡️ בדיקה לפני שליחה (13 בדיקות)
  → שלח: לינק חי / לינק הצעה / PDF / חוזה
```

**חמישה מסלולי שליחה**, כל אחד קורא ל-`recordQuote()` ואז ל-`verifySend()`.

## הלינק החי — הרעיון החזק במערכת

הלקוח מקבל דף אינטראקטיבי, לא PDF. הוא יכול **לבטל סעיפים ולשנות כמויות**, והמחיר מתעדכן. כל פעולה נרשמת ב-Firestore:

```js
{a:'open'} · {a:'off',n:'שם הסעיף',p:מחיר} · {a:'qty',n,from,to} · approvedAt
```

**זה מה שמאפשר** את "מה הלקוחות מורידים", את "🔥 קורא עכשיו", ואת אחוז הסגירה האמיתי.

## מעקב חזרות

קצב אוטומטי: **1 → 3 → 7 → 14 → 30 ימים** מהשליחה. כל "חייגתי" מזיז לשלב הבא. נשמר ב-`followLog`.

---

# 7 · מבנה הנתונים

## 56 מפתחות אחסון · העיקריים

```js
catalog      // מערך 12 קטגוריות, כל אחת {id,title,emoji,items:[]}
quotes       // עד 200. {id,ts,no,client,phone,project,grand,kinds:[],snap:{lines:[]}}
sentLinks    // עד 300. {id,no,client,phone,total,ts,live}
clients      // {id,name,phone,project,address,notes,status,dueDate,createdAt,...}
subItems     // {catId:[סעיפים שהמשתמש הוסיף]}
customItems  // סעיפים בקטגוריית "אחר"
followLog    // {linkId:{n:ניסיונות,at:מתי,done:bool}}
qseq         // מונה מספרי ההצעות
```

## סעיף במחירון

```js
{id:'dm1', name:'הריסת קיר בלוקים/בטון לא תומך',
 price:120, unit:'מ"ר', range:'80–160 ₪ / מ"ר', hot:true,
 // בזמן ריצה:
 selected:true, qty:12, laborMode:false, perm:true, priceAt:ts}
```

**יחידות:** `מ"ר · מ"א · מ"ק · יח׳ · נקודה · גלובלי · חודשי · יום`
`גלובלי` = מחיר קבוע לכל העבודה, ללא תלות בכמות.

## 12 הקטגוריות (644 סעיפים)

| id | סעיפים | שם |
|---|---|---|
| demo | 75 | הריסה ופינוי |
| plumb | 86 | אינסטלציה |
| floor | 52 | ריצוף, חיפוי ואיטום |
| drywall | 18 | גבס ותקרות |
| paint | 24 | שפכטל וצבע |
| elec | 67 | חשמל |
| comm | 24 | תקשורת ומולטימדיה |
| finish | 20 | עבודות גמר ועיצוב פנים |
| alum | 80 | נגרות, מסגרות ואלומיניום |
| plan | 62 | תכנון, ניהול ורישוי |
| struct | 71 | שלד ותשתית כבדה |
| outdoor | 65 | פיתוח חוץ וסביבה |

`custom` (אחר) נוספת דינמית ב-`allCats()`.

---

# 8 · חישובים ולוגיקה עסקית

## שרשרת הסכום

```js
subtotal()      = Σ(price × qty) + matExtra()
discountAmt()   = subtotal × discount%
afterDiscount() = subtotal − discountAmt
vat()           = afterDiscount × vatPct%      // 18%
grand()         = afterDiscount + vat
advanceAmt()    = grand × advance%
```

## `realQuotes()` — מקור אמת אחד לספירה ⚠️ קריטי

```js
realQuotes(){
  return (Store.get('quotes',[])||[]).filter(q=>
    q&&((q.snap&&q.snap.lines&&q.snap.lines.length)||+q.grand>0));
}
```

רשומות מלפני v14.1 נשמרו בלי סעיפים. **חמישה מודולים** נשענים על הפונקציה הזו: `reportData` · `quoteTotals` · `insights` · `priceIndex` · `usageStats`.

**לפני v18.2 כל מסך ספר אחרת** — הדוח הראה 9 הצעות ו-0% סגירה, המעקב הראה 3 ו-33%. אל תחזיר ספירה עצמאית לשום מסך.

## אחוז סגירה

```js
conv = sent ? Math.round(closed/sent*100) : 0
```

**מול מה שנשלח, לא מול מה שהופק.** הצעה שנבנתה ולא נשלחה אינה כישלון.

## איחוד הצעות ב-`_recordQuote`

```js
// 1. לפי מספר הצעה
let exIdx=qs.findIndex(q=>q&&q.no&&q.no===_no);
// 2. גיבוי: אותו לקוח + אותו סכום + אותו יום
if(exIdx<0&&nm&&g>0){
  exIdx=qs.findIndex(q=>String(q.client).trim()===nm &&
    Math.abs((+q.grand||0)-g)<1 && (Date.now()-(+q.ts||0))<DAY);
}
```

**המונה לא מתקדם בשמירה.** קודם כן, ולכן תזכורת על אותה הצעה יצרה רשומה חדשה.

## `laborMode` — "התקנה בלבד"

**משנה נוסח בלבד. לא נוגע במחיר.** יהודה קובע את המחיר במחשבון; לפעמים יש אספקה ולפעמים לא, וזו החלטה שלו. גרסאות מוקדמות הקטינו את המחיר לפי אחוז עבודה — הוסר ב-v14.9 עם מיגרציית שחזור.

## `invalidate(what)` — ביטול מטמונים

```js
invalidate(what){
  const all=!what;
  if(all||what==='links'){this._trackData=null;this._trkLoading=false;}
  if(all||what==='quotes'){this._usSig=null;this._us=null;this._pxSig=null;this._px=null;}
  if(all||what==='catalog'){this._usSig=null;this._us=null;}
  this._preflight=null;
}
```

**14 מקומות קוראים מהמטמונים.** כל כתיבה לנתונים חייבת לקרוא לזה, אחרת מסכים מציגים מספרים ישנים.

---

# 9 · הרשאות משתמשים

**אין משתמשים מרובים.** מכשיר אחד = בעלים.

שכבת הגנה יחידה: **קוד מנהל 4 ספרות** (hash ב-`pinH`, ברירת מחדל `PIN_FALLBACK`).

| | |
|---|---|
| יהודה | **3396** |
| ארתור | **3389** |

מגן על: אזור ההגדרות · עוזר ה-AI · פעולות הרסניות.

**זו הגנה מפני מי שלקח את הטלפון, לא מפני מתכנת.** הקוד רץ בדפדפן ואפשר לעקוף אותו. אל תבטיח יותר מזה.

---

# 10 · אינטגרציות חיצוניות

| שירות | תפקיד | הערות |
|---|---|---|
| **Firebase Firestore** | סנכרון בין מכשירים | `appdata/yehuda` · `appdata/artur` — **0 ערכים משותפים** |
| **Gemini API** | AI ראשי | מפתח ב-`geminiKey` |
| **Claude API** | AI גיבוי | `claudeKey` |
| **Netlify** | אירוח | גרירת ZIP |
| **GitHub Pages** | אירוח חלופי | ⚠️ שתי האפליקציות על אותו דומיין |
| **Google Drive** | גיבוי ידני | OAuth ב-`gClientId` |
| **WhatsApp** | שליחה | `wa.me` |
| **jsPDF + html2canvas** | הפקת PDF | CDN |
| **QRCode.js** | קוד QR ב-PDF | הוסר מארתור (אין לו אתר) |

---

# 11 · מבנה קבצים

```
index.html            719 KB   הכל
sw.js                   3 KB   מטמון: ys-cache-v18.2
version.json                   {"version":"18.2"}
manifest.webmanifest
_headers
icon-192.png / icon-512.png / icon-180.png
README.md
```

## בתוך index.html

```
<head>          meta, manifest, גופנים
<style>         109 KB · CSS
                  :root — משתני מערכת העיצוב
                  ...
                  בסוף: dockwrap, זכוכית, מספרים — כללים שחייבים לנצח
<body>
<script>
  CATALOG       644 סעיפים
  LOGO_APP / LOGO_PDF   base64
  Store         אחסון
  Sync          Firestore
  AI            Gemini/Claude
  App = {216 מתודות}
  מיגרציות + App.init()
```

---

# 12 · החלטות ארכיטקטוניות

| החלטה | נימוק |
|---|---|
| **קובץ אחד, בלי build** | רמי עובד מאנדרואיד בלי terminal. אילוץ, לא העדפה. |
| **namespace `ys:` / `ar:`** | שתי אפליקציות על אותו דומיין חלקו localStorage |
| **`innerHTML` מלא** | פשטות. אין framework, אין virtual DOM |
| **`realQuotes()` יחיד** | חמישה מסכים ספרו אחרת |
| **`invalidate()` יחיד** | 14 קוראים, 1 ניקה |
| **מערכת עיצוב** | 30 גדלי גופן ו-19 רדיוסים → 11 ו-8 |
| **מנועים נפרדים לחישוב ולתצוגה** | `homeState()` מחשב, `homeView()` מציג — בדיק |
| **בדיקות לפני שליחה** | node --check + ספירת div + ספירת סוגריים CSS |
| **`laborMode` = נוסח בלבד** | המחיר הוא החלטה עסקית של הקבלן |
| **טווח מחירים בכל סעיף** | `range` מציג את הטווח המקובל בשוק |

---

# 13 · הנחות יסוד

1. **משתמש יחיד למכשיר.** אין התנגשויות עריכה.
2. **הטלפון הוא הפלטפורמה.** רוחב 360-430px.
3. **עברית RTL בלבד** (למעט תרגום ההצעה ללקוח).
4. **אופליין הוא ברירת מחדל.** רק AI וסנכרון דורשים רשת.
5. **המחירון הוא הנכס.** 644 סעיפים שאף מתחרה לא מגיע אליו.
6. **יהודה לא קורא — הוא סורק.** משפטים קצרים, מספרים גדולים.
7. **סביבת טסטים.** נתונים כפולים בצילומי מסך אינם בהכרח באגים — **שאל לפני שאתה מכריז**.

---

# 14 · מגבלות ידועות

| מגבלה | פירוט |
|---|---|
| **קוד 4 ספרות** | לא מגן מפני מתכנת |
| **תקרת אחסון** | quotes 200 · sentLinks 300. מעבר לזה נמחקות בשקט |
| **Firestore 1MB** | לכן `quotes` נשלח רזה |
| **עריכה מקבילה** | אותו לקוח בשני מכשירים = הכתבה אחרונה מנצחת |
| **אותו דומיין** | ה-namespace מגן, אבל **אל תשנה את מבנה התיקיות** |
| **127 `!important`** | נחוצים. מיפיתי — כמעט כולם דורסים בכוונה |
| **14 בוררי CSS כפולים** | ההגדרות משלימות, לא סותרות. אל תמזג |
| **`settingsModal` 10K תווים** | ענק אבל עובד. האקורדיון מרסן |
| **163 `catch` ריקים** | מול 14 שרושמים ליומן |

---

# 15 · באגים ידועים

**אין באג פתוח ידוע ב-v18.2.**

## תבנית הכשל שחזרה 6 פעמים — קרא את זה

**הוספת מנגנון שני לצד מנגנון קיים:**

| # | מה | תוצאה |
|---|---|---|
| 1 | שני מאזיני `popstate` | כפתור חזרה שבור |
| 2 | שתי מערכות אקורדיון | כל המסכים התקפלו |
| 3 | שני מאזינים על כפתור AI | "ביטול" פתח בכל זאת |
| 4 | "הקטגוריות שלך" מעל 12 קטגוריות | מסך כפול |
| 5 | היסטוריה גם בבית וגם בהצעה | כפילות |
| 6 | 5 קיצורים גם בבית וגם בתפריט | בלבול |

**לפני שאתה מוסיף מנגנון — חפש אם הוא כבר קיים.** יש סקריפט: `dupcheck.py` (סופר מאזינים, בוררי CSS, מתודות כפולות, פיזור פעולות).

## תבנית שנייה: תיקון שהסתיר נתונים

פעמיים סינון "בערך" הסתיר נתונים תקינים:
- `!q.snap` הסתיר הצעות עם סכום (v18.0 → תוקן ב-18.1)
- הסינון נבדק רק על מה שרציתי לתפוס, לא על מה שרציתי לשמור

---

# 16 · משימות פתוחות (TODO)

## 🔴 עדיפות גבוהה

**1. ארכיון הצעות / גיבוי Drive**
`quotes` מוגבל ל-200. אחרי זה הישנות נמחקות **בשקט**, ואיתן זיכרון המחירים והתובנות.
רמי הציע סנכרון Drive — זה פותר את זה בצורה נקייה יותר מארכיון ידני. **לא מומש.**

**2. כיול המחירון**
644 סעיפים במחירים שאני קבעתי בהערכה. יהודה לא עבר עליהם.
נוסה עדכון דרך Gemini — הוא **החזיר את אותם מחירים בדיוק** (העתקה, לא תמחור). קיים כלי ידני: הגדרות ← 📥 הדבק רשימת מחירים מ-AI.

## 🟡 בינונית

**3. ארתור על v2.4** — פיגור של 16 גרסאות. הועברו: לשוניות, מרכז בית, זכוכית, מערכת עיצוב.
**לא הועברו:** מעקב חזרות · תובנות · חיפוש בהצעות · יומן פרויקטים · `realQuotes` · `invalidate` · ביטול פעולה.

**4. איחוד וואטסאפ** — 12 נקודות בקוד. שתיים עברו ל-`openWa()` (עם נפילה כשהדפדפן חוסם), עשר נשארו `window.open`. **רמי ביקש במפורש להחזיר את העשר** — אל תיגע בלי אישור.

**5. `catch` ריקים** — 163. להוסיף `logErr` בנתיבים שנוגעים בנתונים.

## 🟢 נמוכה

**6. תג "מחיר לא אומת"** — נקודה כתומה על סעיף שלא נבדק. פחות רלוונטי אחרי זיכרון המחירים.

**7. ארכוב במקום מחיקה** — 🗑️ במעקב מוחק לצמיתות.

---

# 17 · שיפורים עתידיים

**פרויקט נפרד שנדון ולא נבנה:** משיכת חשבוניות/קבלות ממייל יהודה → תיקיות בדרייב לפי ספק + טבלת ריכוז. **אפיון מלא נמסר.** דורש Gmail API + Drive API + חילוץ נתונים מ-PDF (3 שכבות: טקסט → OCR → AI). **אל תדחוף את זה לתוך אפליקציית ההצעות.**

רעיונות שלא נבדקו: השוואת הצעה מול ביצוע בפועל · מעקב תשלומים ומקדמות · ניהול ספקים.

---

# 18 · קוד קריטי שחייבים להבין

## 1 · `Store` — ה-namespace

אם `NS` נשבר, **כל הנתונים נעלמים** (הם עדיין שם, במפתחות אחרים).

## 2 · `_recordQuote()`

הלב. שומר את ההצעה, מאחד לפי מספר/לקוח, מעדכן לקוח, מקדם מונה. **כל שינוי כאן משפיע על 5 מסכים.**

## 3 · `realQuotes()` + `invalidate()`

שני מקורות האמת. **אל תעקוף אותם.**

## 4 · מאזין `popstate` — 5 שלבים

```
מודל → חיפוש → קטגוריות פתוחות → לשונית → יציאה
```

**מאזין אחד בלבד.** נבדק ב-16 תרחישים.

## 5 · `dockwrap` — רצועת התחתית

הסל, פס הסכום והלשוניות בשכבת flex אחת. `measureDock()` מודד את הגובה בפועל ומזין ל-`--dock-h`.
**קודם היה חישוב ידני בפיקסלים** — וכשפס הסכום גדל, הסל כיסה את כפתור השליחה.

## 6 · כללי CSS בסוף הגיליון

`dockwrap` · זכוכית · המספרים בכרטיסים — **חייבים להישאר אחרונים**. כלל מוקדם דרס להם את הצבע והמספרים נעלמו.

---

# 19 · תהליך פיתוח — עבוד ככה

## עריכה

```python
import io
P='index.html'
h=io.open(P,encoding='utf-8',newline='').read()
def rep(old,new,label,count=1):
    global h
    assert h.count(old)==count,'FAIL[%s] got %d'%(label,h.count(old))
    h=h.replace(old,new)
io.open(P,'w',encoding='utf-8',newline='').write(h)
```

**`assert` חובה.** אם הטקסט לא נמצא בדיוק — עצור, אל תנחש.
**עברית מופיעה גם כ-`\uXXXX`.** חפש בשתי הצורות.

## בדיקה לפני כל אריזה

```python
# 1. JS
for block in re.findall(r'<script(?![^>]*\bsrc=)[^>]*>(.*?)</script>',h,re.S):
    subprocess.run(['node','--check',tmp])
# 2. div מאוזן
# 3. CSS: count('{')==count('}')
# 4. 3396 לא בקוד · אין "קבלן רשום" · popstate פעם אחת
```

**חלץ פונקציות ובדוק אותן ב-node** לפני שאתה שולח. זה תפס באגים אמיתיים: `-50` שהפך ל-50, "כהן" עם שני טלפונים שמוזג, סעיף שנכנס בטעות ל"בלי התנגדות".

## אריזה

```bash
sed -i "s/VERSION='18.2'/VERSION='18.3'/" index.html
sed -i "s/ys-cache-v18\.[0-9]*/ys-cache-v18.3/" sw.js
echo '{"version":"18.3"}' > version.json
# + פסקה במדריך (g30 "מה חדש")
zip -qr yehuda-shipuzim-app-v18.3.zip .
```

**⚠️ אמת את גודל הקובץ אחרי ההעתקה.** פעמיים `index.html` יצא 0 בתים.

---

# 20 · חוקי ברזל

1. **לעולם לא "קבלן רשום" ולא מספר רישום 40998** — בשום מסמך, אתר, כרטיס או הצעה.
2. **הקוד 3396 לא מופיע בקוד המקור** — רק hash.
3. **`fire and forget` אסור.** רמי אמר את זה במפורש. תמיד אמת בקוד, לא בדיווח ההרצה.
4. **תקן בלי לשבור.** כל שינוי נבדק מול מה שכבר עובד.
5. **`str.replace(...,1)` להחלפה יחידה.**
6. **`io.open` עם UTF-8** לכל עריכה.

---

# 21 · פרטי העסק

## יהודה שיפוצים
```
יהודה יעקובוב · 054-5718316 · ח.פ 311834998
yuda090581@gmail.com · yehuda-shipuzim.netlify.app
Firebase: yehuda-shipuzim · doc: yehuda · NS: ys: · קוד: 3396
צבע: אדום #ed1c24 · קידומת: YS-
```

## ארתור שיפוצים
```
ארתור ישראילוב · 054-338-7568 · ח.פ 321059800
snap.artur@gmail.com · אין אתר
Firebase: artur-shipuzim · doc: artur · NS: ar: · קוד: 3389
צבע: כחול #2ba8e0 · קידומת: AR- · גרסה: v2.4
```

**⚠️ שני שדות Firebase הוסרו מארתור** (`messagingSenderId`, `appId` — היו של יהודה). Firestore עובד בלעדיהם.

## פורמט ה-PDF
לוגו ימין + פרטי קבלן · כותרת "הצעת מחיר / QUOTATION" · מספר `YS-YYYYMMDD-NNN` · תוקף 30 יום · טבלה: #, תיאור, כמות, יחידה, מחיר, סה"כ · מקטעים לפי קטגוריה עם סיכומי ביניים · מע"מ 18% · חתימות · QR.

**"פאושלי" אסור** — תמיד "גלובלי".

---

# 22 · מצב הפרויקט

## יהודה — v18.2 · **~92%**

| תחום | |
|---|---|
| מחירון 644 סעיפים | 100% |
| בניית הצעה ו-PDF | 100% |
| לינק חי ומעקב | 100% |
| מרכז בית ולשוניות | 100% |
| מעקב חזרות | 100% |
| תובנות ודוחות | 95% |
| יומן פרויקטים | 90% |
| עיצוב ועקביות | 90% |
| כיול מחירון | **20%** ⚠️ |
| ארכיון/Drive | **0%** ⚠️ |

## ארתור — v2.4 · **~70%**
פיגור של 16 גרסאות. המחירון עדיין במחירים של יהודה.

---

# 23 · Next Steps

**1.** תן ל-v18.2 לרוץ שבוע בשימוש אמיתי. הפאנל "מה הלקוחות מורידים" מעניין רק אחרי 5-6 הצעות.

**2.** גיבוי Drive → פותר את תקרת 200 ההצעות.

**3.** העבר לארתור את 16 הגרסאות.

**4.** כיול המחירון — הסעיפים החמים בלבד (מסומנים `hot:true`).

---

# 24 · איך לעבוד עם רמי

- **הוא בודק כל גרסה ומדווח בצילומי מסך.** קרא אותם בעיון — הם מדויקים.
- **הוא תופס באגים שאתה מפספס.** `messagingSenderId` של יהודה בארתור נשאר עד שהוא שאל.
- **כשהוא אומר "לא הבנתי" — פשט.** בלי מונחים טכניים.
- **הוא מבחין בין באג לרעש טסטים.** שאל לפני שאתה מכריז.
- **תקן שורש, לא סימפטום.** הוא מזהה טלאים.
- **עברית, ישירות, בלי התלהבות מזויפת.**

---

*v18.2 · JS תקין · div 14/14 · CSS 1155/1155 · אפס כפילות ניווט*
